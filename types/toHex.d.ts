export default toHex;
/**
 * 返回将指定颜色（颜色英文名称、RGB格式或者 HSL 格式色值）转化成 16 进制色值
 * ========================================================================
 * @method toHex
 * @since 0.4.0
 * @param {String} color
 * @return {String|Boolean}
 */
declare function toHex(color: string): string | boolean;
