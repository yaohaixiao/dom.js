export default encodeHTML;
/**
 * 转义字符串中的 HTML 标签
 * ========================================================================
 * @param {String} str - 需要转义的字符串
 * @returns {String}
 */
declare function encodeHTML(str: string): string;
