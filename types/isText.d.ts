export default isText;
/**
 * 判断 DOM 元素是否未文本节点
 * ========================================================================
 * @method isText
 * @param {HTMLElement} el
 * @return {Boolean}
 */
declare function isText(el: HTMLElement): boolean;
