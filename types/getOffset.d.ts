export default getOffset;
/**
 * 获取 DOM 节点相对于窗口的 left 和 top 值
 * ========================================================================
 * @method getOffset
 * @param {HTMLElement} el - DOM 节点
 * @returns {Object|null}
 */
declare function getOffset(el: HTMLElement): any | null;
