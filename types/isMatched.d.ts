export default isMatched;
/**
 * 检测 DOM 节点是否为符合检测条件的节点
 * ========================================================================
 * @method isMatched
 * @param {HTMLElement} el
 * @param {Function|String} filter
 * @return {Boolean}
 */
declare function isMatched(el: HTMLElement, filter: Function | string): boolean;
