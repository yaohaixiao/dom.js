export default canPosition;
/**
 * 判断 DOM 元素是否可以定位（可见且在当前当前文档中）。
 * ========================================================================
 * @method canPosition
 * @param {HTMLElement} el
 * @return {Boolean}
 */
declare function canPosition(el: HTMLElement): boolean;
