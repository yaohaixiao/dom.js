section.section
  header.section__header
    h3.section__h3 after(el, reference)
  div.section__content
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd DOM Insertion
    p after() 方法用来将在指定 DOM 节点后添加子节点或者将 HTML 字符转化成节点添加到后面。
    h4.section__h4 Parameters
    h5.section__h4 el
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement|String
    p （必须）el 参数指定要插入的 DOM 元素或者 HTML 字符。
    h5.section__h4 reference
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement
    p （必须）reference 指定 DOM 节点。
    pre.section__pre
      code.section__.
        &lt;ul id="list" class="list"&gt;
            &lt;li class="item about"&gt;About&lt;/li&gt;
        &lt;/ul&gt;

        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 after 单个方法
        import after from '@yaohaixiao/dom.js/after'

        const $list = DOM.byId('#list')
        const $menu = DOM.html('&lt;ul&gt;&lt;li class="item project"&gt;Projects&lt;/li&gt;&lt;/ul&gt;')

        // 将插入 html 插入到 $list 节点后面
        DOM.after('&lt;ul&gt;&lt;li id="home" class="item active"&gt;Home&lt;/li&gt;&lt;/ul&gt;', $list)

        // 将创建的 $menu 节点插入到 $list 节点后面
        after($menu, $list)
