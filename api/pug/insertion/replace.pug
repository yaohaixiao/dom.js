section.section
  header.section__header
    h3.section__h3 replace(el, reference)
  div.section__content
    h4.section__h4 Since
    p.section__dd 1.1.0
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd DOM Insertion
    p replace() 方法用来将 DOM 节点或者 HTML 字符转化成 DOM 节点，已替换指定的 DOM 节点。
    h4.section__h4 Parameters
    h5.section__h4 el
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement|String
    p （必须）el 参数指定要插入的 DOM 元素或者 HTML 字符。
    h5.section__h4 reference
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement
    p （必须）reference 指定要替换的 DOM 节点。
    pre.section__pre
      code.section__.
        &lt;ul id="list" class="list"&gt;
            &lt;li class="item about"&gt;About&lt;/li&gt;
        &lt;/ul&gt;

        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 replace 单个方法
        import replace from '@yaohaixiao/dom.js/replace'

        const $list = DOM.byId('#list')
        const $menu = DOM.html('&lt;ul&gt;&lt;li class="item project"&gt;Projects&lt;/li&gt;&lt;/ul&gt;')

        // 用 html 字符转化的 DOM 节点 替换 $list 节点
        DOM.replace('&lt;ul&gt;&lt;li id="home" class="item active"&gt;Home&lt;/li&gt;&lt;/ul&gt;', $list)

        // 用动态创建的 $menu 节点替换 $list 节点
        replace($menu, $list)
