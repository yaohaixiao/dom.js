section.section
  header.section__header
    h3.section__h3 getPrevSiblings(el)
  div.section__content
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Selecting Siblings
    p getPrevSiblings() 方法用来获取 DOM 元素的前面所有邻居元素节点。
    h4.section__h4 Parameters
    h5.section__h4 el
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd String
    p （必须）el 参数指定 DOM 元素。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement
    p 返回 DOM 元素的前面所有邻居元素节点的数组，如果没有则返回空数组。
    pre.section__pre
      code.section__code.
        &lt;ul id="list" class="list"&gt;
            &lt;li id="home" class="item active"&gt;Home&lt;/li&gt;
            &lt;li class="item blog"&gt;Blog&lt;/li&gt;
            &lt;li class="item"&gt;Projects&lt;/li&gt;
            &lt;li class="item about"&gt;About&lt;/li&gt;
        &lt;/ul&gt;

        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 getPrevSiblings 单个方法
        import getPrevSiblings from '@yaohaixiao/dom.js/getPrevSiblings'

        const $list = DOM.byId('#list')
        const $about = DOM.getEl('.about')

        DOM.getPrevSiblings($list) // ->[]
        getPrevSiblings($about)
        // -> [
        //   &lt;li class="item"&gt;Projects&lt;/li&gt;,
        //   &lt;li class="item blog"&gt;Blog&lt;/li&gt;,
        //   &lt;li id="home" class="item active"&gt;Home&lt;/li&gt;
        // ]
