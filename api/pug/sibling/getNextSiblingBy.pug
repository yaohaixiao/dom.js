section.section
  header.section__header
    h3.section__h3 getNextSiblingBy(el, filter)
  div.section__content
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Selecting Siblings
    p getNextSiblingBy() 方法用来通过过滤器获取 DOM 元素匹配的后面的邻居元素节点。
    h4.section__h4 Parameters
    h5.section__h4 el
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement|Text
    p （必须）el 参数指定 DOM 元素(HTMLElement 或者 Text 类型)。
    h5.section__h4 filter
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Function|String
    p （必须）filter 参数指定用于过滤的回调函数或者选择器字符串。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement|Text
    p 返回 el 元素的下一个邻居元素节点。
    pre.section__pre
      code.section__code.
        &lt;div class="section"&gt;Section Content&lt;/div&gt;
        文本内容
        &lt;ul id="list" class="list"&gt;
            &lt;li id="home" class="item active"&gt;Home&lt;/li&gt;
            &lt;li class="item blog"&gt;Blog&lt;/li&gt;
            &lt;li class="item"&gt;Projects&lt;/li&gt;
            &lt;li class="item about"&gt;About&lt;/li&gt;
        &lt;/ul&gt;

        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 getNextSiblingBy 单个方法
        import getNextSiblingBy from '@yaohaixiao/dom.js/getNextSiblingBy'

        const $section = DOM.byClass('.section')
        const $home = DOM.byId('#home')

        // filter 参数是选择器字符串，获取 HTMLElement 元素
        DOM.getNextSiblingBy($section, '.item') // -> &lt;ul id="list" class="list"&gt;

        // 获取 Text 类型的邻居节点
        getNextSiblingBy($section, (el) => {
           return DOM.isText(el)
        })
        // -> '文本内容'

        // filter 参数是过滤函数，获取 HTMLElement 元素
        getNextSiblingBy($home, (el) => {
          return DOM.hasClass(el, 'about')
        })
        // -> &lt;li class="item about"&gt;About&lt;/li&gt;
