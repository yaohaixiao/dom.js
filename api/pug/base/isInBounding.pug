section.section
  header.section__header
    h3.section__h3 isInBounding(child, parent)
  div.section__content
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Base
    p isInBounding() 方法用来检测某个 DOM 节点的位置是否在另个一个 DOM 节点范围内。
    h4.section__h4 Parameters
    h5.section__h4 child
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement
    p （必须）child 参数指定其中较小的那个 DOM 元素。
    h5.section__h4 parent
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement
    p （必须）parent 参数指定其中较大的那个 DOM 元素。
    h5.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean
    p child 在 parent 范围内，返回 true，否则返回 false。
    pre.section__pre
      code.section__.
        &lt;ul id="list" class="list"&gt;
            &lt;li id="home" class="item active"&gt;Home&lt;/li&gt;
            &lt;li class="item blog"&gt;Blog&lt;/li&gt;
            &lt;li class="item project"&gt;Projects&lt;/li&gt;
            &lt;li class="item"&gt;About&lt;/li&gt;
        &lt;/ul&gt;

        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 isInBounding 单个方法
        import isInBounding from '@yaohaixiao/dom.js/isInBounding'

        const $list = DOM.byId('#list')
        const $home = DOM.byId('#home')

        DOM.isInBounding($home, $list) // -> true
        isInBounding($list, $home) // -> false
