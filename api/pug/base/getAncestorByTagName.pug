section.section
  header.section__header
    h3.section__h3 getAncestorByTagName(el, tagName)
  div.section__content
    h4.section__h4 Since
    p.section__dd 1.10.0
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Base
    p getAncestorByTagName() 方法返回指定 DOM 节点与 tagName 匹配的祖先节点。
    h4.section__h4 Parameters
    h5.section__h4 el
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement
    p （必须）el 参数指定 DOM 元素。
    h5.section__h4 tagName
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd String
    p （必须）tagName 参数指定用于过滤的标签名称字符串。
    h5.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement
    p 返回匹配的祖先节点。
    pre.section__pre
      code.section__.
        &lt;ul id="list" class="list"&gt;
            &lt;li id="home" class="item active"&gt;Home&lt;/li&gt;
            &lt;li class="item"&gt;Blog&lt;/li&gt;
            &lt;li class="item"&gt;Projects&lt;/li&gt;
            &lt;li class="item"&gt;About&lt;/li&gt;
        &lt;/ul&gt;

        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 getAncestorByTagName 单个方法
        import getAncestorByTagName from '@yaohaixiao/dom.js/getAncestorByTagName'

        const $home = DOM.getEl('#home')
        const $blog = DOM.getEl('#blog')

        DOM.getAncestorByTagName($home, 'li')
        // -> &lt;ul id="list" class="list"&gt;

        getAncestorByTagName($blog, 'li')
        // -> &lt;ul id="list" class="list"&gt;
