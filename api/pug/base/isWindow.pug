section.section
  header.section__header
    h3.section__h3 isWindow(o)
  div.section__content
    h4.section__h4 Since
    p.section__dd 1.6.0
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Base
    p isWindow() 方法用来检测当前对象是否为 window 对象。
    h4.section__h4 Parameters
    h5.section__h4 o
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Object
    p （必须）要检测的目标对象。
    h5.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean
    p 返回检测结果。
    pre.section__pre
      code.section__code.
        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 isWindow 单个方法
        import isWindow from '@yaohaixiao/dom.js/isWindow'

        // 浏览器中运行
        DOM.isWindow(window) // -> true
        DOM.isWindow(document) // -> false

        // Node 环境中运行
        isWindow(global) // -> false
