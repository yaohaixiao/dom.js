section.section
  header.section__header
    h3.section__h3 parents(el[, includeSelf = false])
  div.section__content
    h4.section__h4 Since
    p.section__dd 1.7.0
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Base
    p parents() 方法用来获取给定 el 元素的所有父节点，并可以包含自身。
    h4.section__h4 Parameters
    h5.section__h4 el
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement
    p （必须）el 参数指定 DOM 元素或者 DOM 元素的选择器字符串。
    h5.section__h4 includeSelf
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean
    dl.section__dl
      dt.section__dt Default：
      dd.section__dd false
    p （可选）includeSelf 指定之后包含元素自身。默认值：false。
    h5.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Array
    p 返回 el 节点的所有父节点的数组。
    pre.section__pre
      code.section__.
        &lt;ul id="list" class="list"&gt;
            &lt;li id="home" class="item home active"&gt;Home&lt;/li&gt;
            &lt;li class="item blog"&gt;Blog&lt;/li&gt;
            &lt;li class="item project"&gt;Projects&lt;/li&gt;
            &lt;li class="item"&gt;About&lt;/li&gt;
        &lt;/ul&gt;

        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 parents 单个方法
        import parents from '@yaohaixiao/dom.js/parents'

        const $list = DOM.byId('#list')
        const $home = DOM.byId('#home')

        DOM.parents($home)
        // -> [&lt;ul id="list" class="list"&gt;]
        parents('.home', true)
        // -> [
        //  &lt;ul id="list" class="list"&gt;,
        //  &lt;li id="home" class="item active"&gt;Home&lt;/li&gt;
        // ]