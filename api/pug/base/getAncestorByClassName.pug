section.section
  header.section__header
    h3.section__h3 getAncestorByClassName(el, className)
  div.section__content
    h4.section__h4 Since
    p.section__dd 1.10.0
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Base
    p getAncestorByClassName() 方法返回指定 DOM 节点与 className 匹配的祖先节点。
    h4.section__h4 Parameters
    h5.section__h4 el
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement
    p （必须）el 参数指定 DOM 元素。
    h5.section__h4 className
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd String
    p （必须）className 参数指定用于过滤的选择器字符串。
    h5.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement
    p 返回匹配的祖先节点。
    pre.section__pre
      code.section__.
        &lt;ul id="list" class="list"&gt;
            &lt;li id="home" class="item active"&gt;Home&lt;/li&gt;
            &lt;li id="blog" class="item"&gt;Blog&lt;/li&gt;
            &lt;li class="item"&gt;Projects&lt;/li&gt;
            &lt;li class="item"&gt;About&lt;/li&gt;
        &lt;/ul&gt;

        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 getAncestorByClassName 单个方法
        import getAncestorByClassName from '@yaohaixiao/dom.js/getAncestorByClassName'

        const $home = DOM.getEl('#home')
        const $blog = DOM.getEl('#blog')

        DOM.getAncestorByClassName($home, 'list')
        // -> &lt;ul id="list" class="list"&gt;

        getAncestorByClassName($blog, 'list')
        // -> &lt;ul id="list" class="list"&gt;
