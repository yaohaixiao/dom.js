section.section
  header.section__header
    h3.section__h3 getContainingBlock(el)
  div.section__content
    h4.section__h4 Since
    p.section__dd 1.9.0
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Base
    p getContainingBlock() 方法用来获取 DOM 元素的 nodeName 属性值。
    h4.section__h4 Parameters
    h5.section__h4 el
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Node
    p （必须）指定的 HTMLElement 元素。
    h5.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Window
    p 返回 Window 对象。
    pre.section__pre
      code.section__code.
        &lt;style&gt;
          .list {
            transform: scale(1.2);
          }
        &lt;/style&gt;

        &lt;nav id="nav" class="nav"&gt;
          &lt;ul id="list" class="list"&gt;
              &lt;li id="home" class="item active"&gt;Home&lt;/li&gt;
              &lt;li class="item blog"&gt;Blog&lt;/li&gt;
              &lt;li class="item"&gt;Projects&lt;/li&gt;
              &lt;li class="item"&gt;About&lt;/li&gt;
          &lt;/ul&gt;
        &lt;/nav&gt;

        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 getContainingBlock 单个方法
        import getContainingBlock from '@yaohaixiao/dom.js/getContainingBlock'

        const $list = DOM.getEl('#list')
        const $home = DOM.getEl('#home')

        DOM.getContainingBlock($list) // -> 'null

        getContainingBlock($home))
        // -> '&lt;ul id="list" class="list"&gt;&lt;/ul&gt;'
