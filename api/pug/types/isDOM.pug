section.section
  header.section__header
    h3.section__h3 isDOM(el)
  div.section__content
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Node Type Detection
    p isDOM() 方法用来检测测试数据是否为 DOM 类型数据：DOM 节点，TextNode，NodeList 和 DocumentFragment）。
    h4.section__h4 Parameters
    h5.section__h4 el
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Object
    p （必须）el 参数指定待检测的 DOM 元素。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测数据是 DOM 类型数据，false 则表示不是。
    pre.section__pre
      code.section__code.
        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom'
        // 或者单独引用 isTextNode() 方法
        import isDOM from '@yaohaixiao/dom.js/isDOM'

        const $list = document.getElementById('list')
        const $fragment = document.createDocumentFragment()
        const $items = document.querySelectorAll('.item')
        const $text = document.createTextNode('text')

        DOM.isDOM($list) // -> true
        DOM.isDOM($fragment) // -> true
        DOM.isElement($items) // -> false
        isDOM($items) // -> true
        isTextNode($text) // -> true
        isDOM($text) // -> true
