section.section
  header.section__header
    h3.section__h3 isTableElement(node)
  div.section__content
    h4.section__h4 Since
      p.section__dd 1.7.0
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Node Type Detection
    p isTableElement() 方法用来检测 DOM 元素是为 table 列表类的元素。
    h4.section__h4 Parameters
    h5.section__h4 el
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Any
    p （必须）el 参数指定待检测的 DOM 元素。
    h4.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean。
    p true - 表示检测 DOM 元素是 table 列表类的元素，false 则表示不是。
    pre.section__pre
      code.section__code.
        &lt;ul id="list" class="list"&gt;
            &lt;li id="home" class="item active"&gt;Home&lt;/li&gt;
            &lt;li class="item blog"&gt;Blog&lt;/li&gt;
            &lt;li class="item project"&gt;Projects&lt;/li&gt;
            &lt;li class="item"&gt;About&lt;/li&gt;
        &lt;/ul&gt;

        &lt;table class="table"&gt;
           &lt;thead class="thead"&gt;
              &lt;tr&gt;
                   &lt;th&gt;名称&lt;/th&gt;
                   &lt;th&gt;说明&lt;/th&gt;
              &lt;/tr&gt;
           &lt;/thead&gt;
           &lt;tbody class="tbody"&gt;
              &lt;tr&gt;
                   &lt;td&gt;Table&lt;/td&gt;
                   &lt;td&gt;表格标签&lt;/td&gt;
              &lt;/tr&gt;
           &lt;/thead&gt;
        &lt;/table&gt;

        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom'
        // 或者单独引用 isTableElement() 方法
        import isTableElement from '@yaohaixiao/dom.js/isTableElement'

        const $list = DOM.byClass('.list')
        const $table = DOM.byClass(".table")

        DOM.isTableElement($list) // -> false
        isTableElement($table) // -> true
