section.section
  header.section__header
    h3.section__h3 getURLSearchParams(url = location.search)
  div.section__content
    h4.section__h4 Since
    p.section__dd 1.4.0
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Others
    p getURLSearchParams() 方法用来获取 URL 地址字符串中查询字符串解析后的对象数据。
    h4.section__h4 Parameters
    h5.section__h4 url
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd String
    dl.section__dl
      dt.section__dt Default：
      dd.section__dd location.search
    p url 参数指定 URL 地址字符串，默认值：当前 URL 地址的查询字符串。
    h5.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Object
    p 返回 URL 地址字符串中查询字符串解析后的对象数据。
    pre.section__pre
      code.section__code.
        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 getURLSearchParams 单个方法
        import getURLSearchParams from '@yaohaixiao/dom.js/getURLSearchParams'

        const url = 'http://www.yaohaixiao.com/?s=getURLSearchParams&tag=JavaScript'

        // 当前 URL 地址：http://www.yaohaixiao.com/?s=getURLSearchParams
        DOM.getURLSearchParams() // -> {s: 'getURLSearchParams'}

        getURLSearchParams(url) // -> {s: 'getURLSearchParams', tag: 'JavaScript'}
