section.section
  header.section__header
    h3.section__h3 os()
  div.section__content
    h4.section__h4 Since
    p.section__dd 1.5.0
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Base
    p os() 方法用来检测当前操作系统信息，返回操作名称合版本信息。
    h5.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Object
    p 返回检测结果，操作系统名称合版本信息。
    pre.section__pre
      code.section__code.
        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 os 单个方法
        import os from '@yaohaixiao/dom.js/os'

        // Windows 系统浏览器中运行
        DOM.os() // -> {name: 'Windows', version: '10'}

        // Android 系统浏览器中运行
        browser() // -> {name: 'Android', version: '7.1.1'}

        // ios 系统浏览器中运行
        browser() // -> {name: 'iOS', version: '16.6.0'}
