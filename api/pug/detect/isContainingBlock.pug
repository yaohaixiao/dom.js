section.section
  header.section__header
    h3.section__h3 isContainingBlock(el)
  div.section__content
    h4.section__h4 Since
      p.section__dd 1.9.0
    dl.section__dl
      dt.section__dt Category：
      dd.section__dd Detect
    p isContainingBlock() 方法用来检测 DOM 节点用来检测 DOM 元素是否为布局和包含块。
    h4.section__h4 Parameters
    h5.section__h4 el
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd HTMLElement
    p （必须）el 参数指定 DOM 元素。
    h5.section__h4 Returns
    dl.section__dl
      dt.section__dt Type：
      dd.section__dd Boolean
    p DOM 节点为布局和包含块，返回 true，否则为 false。
    pre.section__pre
      code.section__.
        &lt;style&gt;
          .list {
            transform: scale(1.2);
          }
        &lt;/style&gt;

        &lt;ul id="list" class="list"&gt;
            &lt;li id="home" class="item active"&gt;Home&lt;/li&gt;
            &lt;li class="item blog"&gt;Blog&lt;/li&gt;
            &lt;li class="item project"&gt;Projects&lt;/li&gt;
            &lt;li class="item"&gt;About&lt;/li&gt;
        &lt;/ul&gt;

        // 引入 DOM 模块（中的所有工具方法）
        import DOM from '@yaohaixiao/dom.js/dom
        // 引用 isContainingBlock 单个方法
        import isContainingBlock from '@yaohaixiao/dom.js/isContainingBlock'

        const $list = DOM.byId('#list')
        const $home = DOM.byId('#home')

        DOM.isContainingBlock($home) // -> true
        isContainingBlock($home) // -> false
